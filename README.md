# gt-school

Bootstrapped project workspace. The backend is a Docker Compose stack with an
API placeholder, PostgreSQL + pgvector, and Redis queue service. See
backend/README.md to choose and add the API language.

Run npm start to open the themed interactive project menu.
